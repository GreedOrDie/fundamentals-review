package worksheet.ifs;

/**
 * Created by jeff on 12/19/16.
 */
public class IfExamples {

    public static void main(String[] args) {
        double price = 10.00;
        double discount = 0.20;
        int quantity = 3;
        String name = "red shoes";

        // If the price is < 20 print "This is super cheap" else print "This is pricey"


        // If the quantity < 5 print "Order More"


        /* If the discount is between 0 and 0.15 print "Daily Sale"
        if the discount is between 0.15 to 0.25 print "Big sale"
        if the discount is above 0.25 print "Blowout sale"
        other wise print "Invalid sale"
         */


        /* using the % operator determine if the quanity if even or odd
         and print "Even" or "Odd" appropriately
          */


        /* if the product is red shoes and the discount is > 0.10,  or the name isn't red shoes and the discount
        is > 0.30 print "You should buy this.
         */


    }
}
